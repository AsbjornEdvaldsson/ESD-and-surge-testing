The images and CSVs with the same name show the same data. The images are for helping you the reader to navigate to which CSV you would like to mess with. 
The Dicts correspond to the time settings from the scope at the time of reading the data. The names should be similar to those of the CSVs, but if the data looks off, it is probably off.

If you want to conduct multiple consecutive tests, use the program.csv file where every line is "one test procedure". I recomennd using notepad++